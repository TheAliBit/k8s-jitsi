apiVersion: v1
kind: Service
metadata:
  name: prosody
  namespace: jitsi
spec:
  selector:
    app: prosody
  ports:
    - name: xmpp
      port: 5222
      targetPort: 5222
    - name: http
      port: 5280
      targetPort: 5280
  type: ClusterIP
